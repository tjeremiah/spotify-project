import React from 'react';
import ReactDOM from 'react-dom';
//import SpotifyApp from './src/components/SpotifyApp.js'
import App from './App'

ReactDOM.render(
  <App />,
  document.getElementById('app') // make sure this is the same as the id of the div in your index.html
);